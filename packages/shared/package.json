{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared types and tRPC router for aimparency",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@trpc/server": "^11.7.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}