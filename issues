- 'l' does indeed just expand aim. but when aim is just expanded j should skip to the next sibling not step inside sub aims. (we might need to track two different states if we arent already: expanded and selectedSubAim which can be null)
- dragging handles does not lead to handle update on mousemove (just on release). but it should update on mouse/touch move, too. 
- in aim search, tab should tab to the search result list 
- for inconsitencies we should also output the auto-resolve action in the list items. 
- deleting an aim should remove all connections (supporting or supported) to other aims.
- favicon should be a bit more simple and less detailed
- when switching from column view to graph the selected node should be focused smoothly in graph view
- when double clicking to create aim - please reuse existing aim creation modal

---

- when coming from the decessor of the parent aim, k should only step into the subaims when expanded *and* a child is selected (selectedChildIndex != null)
- when switching to graph the view focusses on the selected aim, that's good. However, when we don't wait for the movement to be completed, switching to column and back to graph shows the same animation as if the camera pan was not persisted in memory during the focus animation. It should start where we left of no matter whether user pan or animation pan
- after creating an aim in graph mode, it should be selected. can we have dd work for deletion in graph mode? shortcut? (d => red bg, d again = confirm)
- actually, pressing d in graph mode currently removes stuff as if the column view was open. that's really bad. we need to clearly disable column view shortcut handling when column view is not active (e.g. when in graph view)
